{
  "name": "@app-o11y/root",
  "version": "0.0.0",
  "private": true,
  "description": "Local-first browser session recorder workspace",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r --if-present test"
  },
  "packageManager": "pnpm@10.18.0"
}
